#include<bits/stdc++.h>
using namespace std;

int main(){
  long long n;
  cin >> n;
  long long sum = n * (n + 1) / 2;  // Using the formula to calculate the sum
  cout << sum << endl;
  return 0;
}
